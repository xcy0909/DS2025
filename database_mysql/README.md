前端层View层（frontend)：
  ├── index.html    # 主操作界面
  └── login.html    # 登录界面
  
后端层Controller层(backend)：
  └── app.py        # 处理业务逻辑，路由分发
  ├── models.py     # 数据模型定义
  └── config.py     # 数据库和会话配置

数据库导入文件，在MySQL Workbench 8.0 CE.lnk的数据库导入
  └── database_sql    

  程序需运行app.py
